services:

  app:
    build:
      dockerfile: Dockerfile-poetry
    ports:
      - "8000:8000"
    volumes:
      - images:/app/images
      - logs:/app/logs
      - .:/app # dev mode
    env_file: .env
    restart: always

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - images:/data/images
      - ./static:/data/static
      - logs:/var/log/nginx

volumes:
  images:
  logs: